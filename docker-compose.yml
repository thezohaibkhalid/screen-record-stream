version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: screen-record-stream
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DATABASE_URL_POSTGRES=${DATABASE_URL_POSTGRES}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - NEXT_PUBLIC_BASE_URL=${NEXT_PUBLIC_BASE_URL}
      - ARCJET_API_KEY=${ARCJET_API_KEY}
      - XATA_API_KEY=${XATA_API_KEY}
      - BUNNY_VIDEO_LIBRARY_ID=${BUNNY_VIDEO_LIBRARY_ID}
      - BUNNY_STREAM_ACCESS_KEY=${BUNNY_STREAM_ACCESS_KEY}
      - BUNNY_STORAGE_ACCESS_KEY=${BUNNY_STORAGE_ACCESS_KEY}
    restart: unless-stopped
    networks:
      - app-network

  # Uncomment if you want to run PostgreSQL in Docker
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: screen-record-postgres
  #   environment:
  #     - POSTGRES_USER=${POSTGRES_USER:-postgres}
  #     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
  #     - POSTGRES_DB=${POSTGRES_DB:-screenrecord}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - app-network
  #   restart: unless-stopped

networks:
  app-network:
    driver: bridge

# volumes:
#   postgres_data:

